services:
  bot:
    build: .
    container_name: english-practice-bot
    restart: unless-stopped
    env_file: .env
    environment:
      # Selects .env.production and feeds Settings.app.environment.
      - APP__ENVIRONMENT=production
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
